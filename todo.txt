- On debian 8 (and perhaps 7?) root login is disabled by default, provide option to give weak user to enable root login temporarily then disable it when we finish
- add Wordpress strategy (wp-cli can do this apperantly.)
- http://wp-cli.org/config/